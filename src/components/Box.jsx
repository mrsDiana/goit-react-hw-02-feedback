import styled from 'styled-components';
import {
  grid,
  space,
  typography,
  layout,
  flexbox,
  border,
  shadow,
  color,
} from 'styled-system';
export const Box = styled.div`
  ${grid}
  ${space}
  ${typography}
  ${layout}
  ${flexbox}
  ${border}
  ${shadow}
  ${color}
`;
